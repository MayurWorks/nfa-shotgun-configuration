# Standard Toolkit project root. Folder name comes from the Project's
# Tank Name field, which the project-creator tool sets to the project's
# short code (e.g. "STRM") — see model.py tank_name assignment.

type: "project"

# name of project root as defined in roots.yml / Local Storage "primary"
root_name: "primary"
