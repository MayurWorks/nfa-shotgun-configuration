StudioTemplate.nk — the studio's Nuke starter script.

Referenced by templates.yml's nuke_template_file path and used by the
tk-nuke-template app when a new work file is opened for a task.

This currently contains NFA's original ShotGridTemplate.nk content,
renamed as a starting point. Replace with the studio's own comp starter
script (standard Read/colorspace/viewer/Write node setup, using
aces_1.2 / ACEScg per our OCIO decision) when ready.
