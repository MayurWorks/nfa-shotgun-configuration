Placeholder for StudioTemplate.nk (the studio's Nuke starter script).

Referenced by templates.yml's nuke_template_file path and used by the
tk-nuke-template app when a new work file is opened for a task.

NFA's original ShotGridTemplate.nk was removed when this schema was
rebuilt for our studio's folder convention. Replace this file with the
studio's own comp starter script (standard Read/colorspace/viewer/
Write node setup) when ready.
