# Studio tk-multi-snapshot settings.
# Copied from NFA's config unchanged — snapshot naming reuses our own
# nuke_shot_snapshot / nuke_shot_work template names from templates.yml.

includes:
    - ../app_locations.yml

################################################################################

settings.tk-multi-snapshot.nuke.shot_step:
  template_snapshot: nuke_shot_snapshot
  template_work: nuke_shot_work
  location: "@apps.tk-multi-snapshot.location"
