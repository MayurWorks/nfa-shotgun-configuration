# Studio shot_step environment.
# Trimmed to Nuke only for now — add other DCC engines (Houdini, Maya, etc.)
# as separate includes/settings files when needed, following the same
# pattern as tk-nuke.yml.

description: Apps and Engines related to Shot based work.

################################################################################

includes:
- ./includes/settings/tk-nuke.yml

################################################################################
# configuration for all engines to load in a shot step context

engines:
  tk-nuke: "@settings.tk-nuke.shot_step"

################################################################################
